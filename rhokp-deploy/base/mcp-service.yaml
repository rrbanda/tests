apiVersion: v1
kind: Service
metadata:
  name: rhokp-mcp
  labels:
    app.kubernetes.io/name: rhokp-mcp
    app.kubernetes.io/component: mcp-server
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
      name: http
  selector:
    app: rhokp-mcp
